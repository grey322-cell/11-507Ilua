(7 баллов) Напишите AttributeFilter. Метод GetValidObjects(List<object> objects) 
должен вернуть список только тех объектов, у которых есть хотя бы одно 
свойство с атрибутом [Sensitive].

(7 баллов) Напишите ParallelProcessor: есть список из 1000 разных объектов.
 Разбейте их на 4 чанка (используйте Parallel.For или индексы массива). 
 Каждый поток должен вывести в консоль ThreadID и количество объектов, которые он обработал. 
 Обработкой будет получение типа объекта (сохранять никуда не надо).

(1 балл) Объедините: в каждом потоке ParallelProcessor (задача №2) сначала прогоните 
свою порцию объектов через AttributeFilter (задача №1), и только объекты, 
прошедшие фильтр, выводите в консоль.

